# ai_docs/

Transient AI-generated documentation. Each file is prefixed with the date it
was created (YYYY-MM-DD). These are snapshots in time — the codebase is the
source of truth.

Files here are version-controlled for reference but should not be treated as
authoritative. When in doubt, read the code.
